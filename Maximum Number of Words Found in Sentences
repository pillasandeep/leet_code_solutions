class Solution:
    def mostWordsFound(self, sentences: List[str]) -> int:
        j=0
        for i in sentences:
            x=i.split()
            l=len(x)
            if l>j:
                j=l     
        return j
